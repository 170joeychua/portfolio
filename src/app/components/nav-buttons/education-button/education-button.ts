import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-education-button',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './education-button.html',
})
export class EducationButton {}
