import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-recognitions-button',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './recognitions-button.html',
})
export class RecognitionsButton {}
