import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-experiences-button',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './experiences-button.html',
})
export class ExperiencesButton {}
